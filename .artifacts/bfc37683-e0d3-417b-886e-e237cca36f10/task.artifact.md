# مهام تفعيل الأمان وضبط استدعاءات فيرباس

- `[x]` ضبط تهيئة Firebase Admin
    - `[x]` تحديث `backend/config/firebase.ts` لمعالجة مفتاح الخصوصية
    - `[x]` تحديث سكريبت `set-admin.js` لدعم متغيرات البيئة
- `[x]` ربط المصادقة في الفرونت اند
    - `[x]` تحديث `src/lib/api.js` لإرسال التوكن في الـ Headers
    - `[x]` تعديل `AuthContext.jsx` لضمان تحديث التوكن عند الحاجة
- `[x]` تفعيل الحماية في الباك اند
    - `[x]` تفعيل `authMiddleware` في `userRoutes.ts`
    - `[x]` إضافة تحقق الرتبة (Admin Check) في `adminRoutes.ts`
- `[x]` إصلاح مشاكل ظهور البيانات في لوحة التحكم
    - `[x]` تصحيح ملف الدخول الرئيسي في `index.html` (توجيهه إلى `main.jsx` بدلاً من `main.tsx` الفارغ)
    - `[x]` تحديث `AdminDashboard.jsx` لدعم التحقق المزدوج (Token + Database) وعرض الأخطاء بشكل واضح
    - `[x]` مزامنة رتبة الأدمن تلقائياً عند الدخول في حال وجود الـ Claim في فيرباس
- `[ ]` التحقق والاختبار النهائي
    - `[ ]` التأكد من ظهور قائمة المستخدمين في لوحة التحكم
    - `[ ]` التأكد من عمل الإحصائيات المالية
